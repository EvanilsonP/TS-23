// Basic types
let id: number = 5;
let company: string  = 'Traversy media';
let isPublished: boolean = true;
let x: any = 'Hello';

let ids: number[] = [1, 2, 3, 4, 5];
let arr: any[] = [1, true, 'Hi'];

// Tuple
// Tuple represents a JS array where we can define the data type of each element in the array
let person: [number, string, boolean] = [1, 'Evanilson', true];

// tuple array
let employee: [number, string][];

employee = [
    [1, 'Joel'],
    [2, 'Tess'],
    [3, 'Ellie'],
];